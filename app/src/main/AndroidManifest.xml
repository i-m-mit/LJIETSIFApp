<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mitapps.ljietsifapp">

    <uses-feature
        android:name="android.hardware.camera2"
        android:required="true" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
        </activity>
        <activity android:name=".AcademicInfo"></activity>
        <activity android:name=".ExtraInfo"></activity>
        <activity android:name=".FamilyInfo"></activity>
        <activity android:name=".PersonalInfo"></activity>
        <activity android:name=".ResidentialInfo"></activity>
        <activity
            android:name=".CaptureImageActivity"
            android:label="@string/title_activity_capture_image"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SIFMainActivity"
            android:label="@string/title_activity_sifmain"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>